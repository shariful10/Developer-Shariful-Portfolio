import React from 'react';
import SectionTitle from '../../Shared/SectionTitle/SectionTitle';

const Contact = () => {
    return (
        <section id='contact' className='h-40'>
            <SectionTitle heading={"Contact"} scale='1/12' />
        </section>
    );
};

export default Contact;